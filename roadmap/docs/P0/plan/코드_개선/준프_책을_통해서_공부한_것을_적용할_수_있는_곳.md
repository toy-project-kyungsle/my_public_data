

실제로는 **거의 모든 항목**이 의존성 분리/역전/인터페이스 설계와 관련되어 있습니다. 구체적으로 보겠습니다:

## 10개 항목의 의존성 설계 관련성 분석

### 🎯 **직접적으로 해당** (설계 패턴 핵심)

**3. 순환 의존성 제거** ⭐⭐⭐

- **의존성 분리**: 브릿지 모듈 패턴, re-export 정리
- **의존성 역전**: 순환 참조를 인터페이스로 끊기
- 가장 직접적으로 해당

**6. Zustand 스토어 정리** ⭐⭐⭐

- **의존성 분리**: 서버 상태(React Query) vs UI 상태(Zustand) 분리
- **인터페이스 분리**: 도메인별 스토어 분리
- 명시적으로 "서버 상태와 UI 상태 경계가 불명확" → 의존성 설계 문제

**4. Error Boundary 확장** ⭐⭐

- **인터페이스 분리**: 에러 타입별 계층 설계

```typescript
AppError → NetworkError / ValidationError / ServerError
```

- 에러 처리 책임을 레벨별로 분리

---

### 🔧 **간접적으로 해당** (구현 시 설계 패턴 필요)

**2. 테스트 커버리지 강화** ⭐⭐

- 테스트를 쉽게 만들려면 **의존성 주입** 필수

```typescript
// ❌ 테스트 어려움
function checkout() {
  const result = axios.post('/api/checkout');
}

// ✅ 테스트 가능 (의존성 주입)
function checkout(httpClient: HttpClient) {
  const result = httpClient.post('/api/checkout');
}
```

**10. React Query 최적화** ⭐⭐

- "쿼리 키 팩토리 패턴" 언급 → **의존성 분리** 패턴

```typescript
// 쿼리 키 관리 인터페이스 분리
const queryKeys = {
  products: {
    all: ['products'] as const,
    lists: () => [...queryKeys.products.all, 'list'] as const,
    detail: (id: string) => [...queryKeys.products.all, 'detail', id] as const,
  }
}
```

**1. TypeScript 엄격 모드** ⭐

- 타입을 명시하면 자연스럽게 **인터페이스 설계** 필요

```typescript
// noImplicitAny: true → 인터페이스 정의 강제
interface UserService {
  getUser(id: string): Promise<User>; // any 불가
}
```

---

### ⚙️ **약하게 관련** (설계보다는 성능/품질)

**5. 메모이제이션 전략** ⭐

- React.memo는 "컴포넌트 인터페이스 분리"와 관련
- Props가 명확히 정의되어야 메모이제이션 효과적

```typescript
// 인터페이스가 명확해야 memo 효과적
interface ProductCardProps {
  product: Product; // 명확한 인터페이스
  onAddCart: (id: string) => void;
}
const ProductCard = React.memo<ProductCardProps>(/* ... */);
```

**7. ESLint 규칙 강화** ⭐

- "패키지별 import 제한" → **의존성 방향 강제**

```javascript
// lib-business에서 UI import 금지 → 의존성 역전 강제
{
  rules: {
    'no-restricted-imports': ['error', {
      patterns: ['**/ui-system/**']
    }]
  }
}
```

**8. CI/CD 파이프라인 강화** ⭐

- 설계 패턴과는 무관 (인프라)

**9. 번들 최적화** ⭐

- 코드 스플리팅은 "모듈 분리"와 관련 있지만, 의존성 설계는 아님

---

## 정리: 관련성 순위

|순위|항목|관련성|핵심 패턴|
|---|---|---|---|
|1|순환 의존성 제거|⭐⭐⭐ 직접|의존성 분리, 의존성 역전|
|2|Zustand 정리|⭐⭐⭐ 직접|의존성 분리, 인터페이스 분리|
|3|Error Boundary|⭐⭐ 직접|인터페이스 분리 (에러 계층)|
|4|테스트 커버리지|⭐⭐ 간접|의존성 주입|
|5|React Query|⭐⭐ 간접|의존성 분리 (쿼리 키)|
|6|TypeScript 엄격|⭐ 간접|인터페이스 정의 강제|
|7|메모이제이션|⭐ 약함|컴포넌트 인터페이스|
|8|ESLint 규칙|⭐ 약함|의존성 방향 강제|
|9|CI/CD|-|무관|
|10|번들 최적화|-|무관|

## 결론

**의존성 설계와 직접 관련된 항목**:

- ✅ **3. 순환 의존성** (가장 직접적)
- ✅ **6. Zustand 정리** (상태 분리 설계)
- ✅ **4. Error Boundary** (에러 계층 설계)

**간접적으로 관련된 항목**:

- ⚠️ **2. 테스트 커버리지** (테스트 가능한 설계)
- ⚠️ **10. React Query** (쿼리 키 팩토리 패턴)
- ⚠️ **1. TypeScript** (타입 = 인터페이스)

나머지는 성능/품질/인프라 개선이므로 의존성 설계와는 거리가 있습니다!