
2026-02-07

## 1. `error.handled:false` vs `level:fatal`

### 먼저 비유로 이해하기

식당에 비유해보겠습니다.

```
🍳 주방에서 불이 났다!

시나리오 A: 소방관이 출동해서 진화함 (handled: true)
  → 누군가 catch 블록으로 잡아서 처리한 에러

시나리오 B: 아무도 안 와서 식당이 탔다 (handled: false)
  → 아무도 catch 안 해서 앱이 터진 에러
```

### `error.handled` = "누가 이 에러를 잡았는가?"

```typescript
// ✅ handled: true → 개발자가 직접 잡은 에러
try {
  await fetchUserData();
} catch (error) {
  // 내가 잡았다!
  Sentry.captureException(error);  // → handled: true
  showErrorMessage('다시 시도해주세요');
}
```

```typescript
// ❌ handled: false → 아무도 안 잡은 에러
function renderProduct() {
  const price = product.price.toFixed(2);  // product가 null이면?
  // → Uncaught TypeError: Cannot read properties of null
  // → 아무도 catch 안 함
  // → 앱이 뻗음
  // → Sentry SDK가 마지막에 주워서 전송 (handled: false)
}
```

**`handled: false`가 "fatal"인 이유**: 개발자가 어디서도 잡지 않은 에러라서, 사용자 입장에서는 **앱이 멈추거나 화면이 하얘지는** 경험을 한 것입니다.

### `level:fatal` = "에러의 심각도 라벨"

이건 **사람이 붙이는 라벨**입니다.

```typescript
// 개발자가 직접 "이건 fatal이야"라고 라벨을 붙임
Sentry.captureException(error, { level: 'fatal' });

// 또는 Logger로
Logger.fatal('payment', '결제 시스템 완전 다운');
```

### 둘의 차이 정리

```
┌─────────────────────────────────────────────────────────┐
│          error.handled:false                             │
│          "누가 잡았는가?"에 대한 사실(fact)               │
│                                                         │
│    SDK가 자동으로 판단:                                   │
│    - catch 블록 안에서 captureException() → handled:true │
│    - 아무도 안 잡아서 SDK가 주움 → handled:false          │
│                                                         │
├─────────────────────────────────────────────────────────┤
│          level:fatal                                     │
│          "얼마나 심각한가?"에 대한 의견(opinion)           │
│                                                         │
│    개발자가 직접 지정:                                     │
│    - fatal: 앱 사용 불가 수준                             │
│    - error: 기능 실패                                    │
│    - warning: 잠재적 문제                                 │
│    - info: 참고 정보                                     │
└─────────────────────────────────────────────────────────┘
```

**핵심**: `handled:false`는 SDK가 자동으로 붙이는 **객관적 사실**, `level:fatal`은 개발자가 수동으로 붙이는 **주관적 라벨**입니다.

### 앱과 웹 모두에서 잡는가?

**네, 둘 다 잡습니다.** 방식만 다릅니다:

```
📱 React Native (앱):
   @sentry/react-native가 자동으로 등록:
   → ErrorUtils.setGlobalHandler()  ← RN의 글로벌 에러 핸들러
   → 네이티브 크래시 핸들러 (iOS/Android)
   
   아무도 catch 안 한 JS 에러가 여기로 옴
   → handled: false로 Sentry에 전송

🌐 Web (브라우저):
   @sentry/react가 자동으로 등록:
   → window.onerror  ← 브라우저의 글로벌 에러 핸들러
   → window.onunhandledrejection  ← Promise 에러 핸들러
   
   아무도 catch 안 한 JS 에러가 여기로 옴
   → handled: false로 Sentry에 전송
```

### 대시보드에서는 뭘 써야 하나?

**JS Fatal 발생률**에는 `error.handled:false`를 쓰세요. 이유:

- `level:fatal`은 개발자가 안 붙이면 빠짐 (현재 프로젝트에서 거의 안 붙이고 있음)
- `error.handled:false`는 SDK가 자동으로 판단하니 누락이 없음
- 실제로 사용자에게 영향을 준 에러만 정확히 잡아냄

---

## 2. `error.mechanism:onunhandledrejection` = Unhandled Promise Rejection

### Promise가 뭔지부터

```typescript
// Promise = "나중에 결과를 줄게"라는 약속
const promise = fetch('https://api.athler.com/products');
// 이 시점에서 아직 결과 없음
// 네트워크 요청이 "나중에" 완료됨
```

### 왜 "비동기" 에러인가?

**동기(Synchronous) 에러** = 코드가 실행되는 그 줄에서 바로 터짐:

```typescript
// 1번 줄 실행
const name = user.name;    // 2번 줄 - user가 null이면 여기서 바로 터짐!
console.log('안녕');        // 3번 줄 - 2번에서 터졌으니 여기는 실행 안 됨
```

**비동기(Asynchronous) 에러** = 코드가 실행된 "나중에" 터짐:

```typescript
// 1번 줄: fetch를 호출하고 Promise를 받음
fetch('https://api.athler.com/products');
// 2번 줄: 바로 다음 줄로 넘어감 (기다리지 않음!)
console.log('나는 실행됨');
// 3번 줄: 이것도 실행됨

// ... 시간이 흐른 뒤 ...
// 서버가 응답 실패! 하지만 이미 2번, 3번 줄은 지나갔음
// 이 에러를 누가 잡지? → 아무도 안 잡으면 "unhandled rejection"
```

### 어떤 상황에서 발생하는가?

#### 상황 1: `.catch()` 안 붙인 Promise

```typescript
// ❌ catch 없음 → 서버 에러 시 unhandled rejection
fetch('https://api.athler.com/products')
  .then(res => res.json())
  .then(data => setProducts(data));
  // .catch()가 없다!

// ✅ catch 있음 → handled
fetch('https://api.athler.com/products')
  .then(res => res.json())
  .then(data => setProducts(data))
  .catch(error => showError('상품 로딩 실패'));
```

#### 상황 2: async/await에서 try/catch 안 감쌈

```typescript
// ❌ try/catch 없음 → 서버 에러 시 unhandled rejection
async function loadProducts() {
  const res = await fetch('https://api.athler.com/products');
  const data = await res.json();
  setProducts(data);
}

// ✅ try/catch 있음 → handled
async function loadProducts() {
  try {
    const res = await fetch('https://api.athler.com/products');
    const data = await res.json();
    setProducts(data);
  } catch (error) {
    showError('상품 로딩 실패');
  }
}
```

#### 상황 3: Promise 안에서 throw

```typescript
// ❌ Promise 안에서 throw했는데 catch 없음
new Promise((resolve, reject) => {
  const user = getUser();
  if (!user) {
    throw new Error('사용자 없음');  // → unhandled rejection
  }
  resolve(user);
});
```

### 왜 "비동기"라서 특별한가?

**동기 에러는 `window.onerror`가 잡고, 비동기 에러는 `window.onunhandledrejection`이 잡기 때문입니다.**

```
동기 에러의 여행:
  throw new Error() 
    → 콜스택을 타고 올라감 
      → 아무도 안 잡으면 window.onerror 도착
        → Sentry가 캡처 (mechanism: "onerror")

비동기 에러(Promise)의 여행:
  Promise 내부에서 reject/throw
    → 콜스택이 이미 비어있음 (비동기니까!)
      → window.onerror로 갈 수 없음
        → 대신 window.onunhandledrejection으로 감
          → Sentry가 캡처 (mechanism: "onunhandledrejection")
```

**JS 엔진이 이 둘을 다른 경로로 처리하기 때문에**, Sentry도 다른 `mechanism`으로 분류하는 것입니다.

### 실제 Athler 프로젝트에서의 흐름

```
사용자가 상품 페이지 열음
  → fetchProducts() 호출 (async)
    → 네트워크 끊김!
      → fetch() Promise가 reject됨
        → catch가 없다면?
          → 브라우저: window.onunhandledrejection 발동
          → RN: Promise rejection tracking 발동
            → Sentry SDK가 캡처
              → mechanism: "onunhandledrejection"
              → handled: false
                → Sentry Issues에 기록됨
```

### 정리

```
┌──────────────────────────────────────────────────────┐
│                   에러 발생                            │
├────────────────────┬─────────────────────────────────┤
│   동기 에러         │   비동기 에러 (Promise)          │
│   throw new Error() │   Promise.reject()              │
│                     │   async 함수 안에서 throw       │
├────────────────────┼─────────────────────────────────┤
│   잡는 곳:          │   잡는 곳:                       │
│   try/catch         │   .catch() 또는 try/catch       │
├────────────────────┼─────────────────────────────────┤
│   아무도 안 잡으면:  │   아무도 안 잡으면:              │
│   window.onerror    │   window.onunhandledrejection   │
├────────────────────┼─────────────────────────────────┤
│   Sentry:           │   Sentry:                       │
│   mechanism:onerror │   mechanism:onunhandledrejection│
│   handled:false     │   handled:false                 │
└────────────────────┴─────────────────────────────────┘
```