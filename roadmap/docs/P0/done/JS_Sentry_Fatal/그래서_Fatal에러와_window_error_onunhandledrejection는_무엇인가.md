
2026-02-07

울지 마세요! 천천히 아주 처음부터 설명하겠습니다.

---

## 1. "SDK가 주웠다"는 게 무슨 말?

**맞습니다.** 사용자는 이미 에러를 경험한 뒤입니다.

순서를 그려보면:

```
시간 순서:

1️⃣ 에러 발생
   → product.price.toFixed(2)  // product가 null!
   
2️⃣ 앱이 터짐 (사용자가 경험)
   → 화면이 하얘지거나
   → ErrorBoundary의 "다시 시도" 페이지가 뜨거나
   → 앱이 강제 종료되거나
   
3️⃣ Sentry SDK가 캡처 (이건 사용자 모르게 백그라운드에서)
   → "아, 에러가 났구나. 센트리 서버로 보내야지"
   → 인터넷으로 센트리 서버에 전송
   
4️⃣ 센트리 대시보드에 에러가 기록됨
   → 개발자가 나중에 확인
```

"SDK가 주웠다"는 비유는 이런 의미입니다:

```
개발자 코드: "나는 이 에러 모른다" (catch 안 함)
    ↓
JS 엔진: "아무도 안 잡네... 위로 올린다"
    ↓
글로벌 에러 핸들러: "아무도 안 잡네..."
    ↓
Sentry SDK: "내가 여기서 주울게!" ← 마지막 안전망
    ↓
Sentry 서버로 전송
```

SDK는 **병원의 CCTV** 같은 것입니다. 환자(에러)가 쓰러진 걸 막아주진 못하지만, "누가, 언제, 어디서 쓰러졌는지" 기록해서 의사(개발자)에게 알려주는 역할입니다.

---

## 2. 콜스택이 뭐고, 동기 에러가 어떻게 올라가는 건지

### 먼저, 콜스택(Call Stack)이 뭔지

**콜스택 = 접시 쌓기**입니다.

식당에서 접시를 쌓는다고 생각해보세요:

```
함수를 호출하면 → 접시를 하나 올림
함수가 끝나면   → 접시를 하나 내림
```

코드로 보겠습니다:

```typescript
function a() {
  b();        // b를 호출 → 접시 올림
}

function b() {
  c();        // c를 호출 → 접시 올림
}

function c() {
  console.log('안녕!');  // 실행 완료 → 접시 내림
}

a();  // 시작! → 접시 올림
```

이걸 그림으로 보면:

```
시간 →→→→→→→→→→→→→→→→→→→→→→→→→→→→

a() 호출     b() 호출     c() 호출     c() 끝      b() 끝      a() 끝
                                        
  ┌───┐       ┌───┐       ┌───┐                                
  │   │       │   │       │ c │ ←맨위   ┌───┐       
  │   │       │ b │       │ b │         │ b │       ┌───┐
  │ a │       │ a │       │ a │         │ a │       │ a │       (비어있음)
  └───┘       └───┘       └───┘         └───┘       └───┘       

  접시1개      접시2개      접시3개       접시2개     접시1개      접시0개
```

### 에러가 콜스택을 "타고 올라간다"는 것

`c()` 안에서 에러가 나면, 접시를 하나씩 내리면서 **"혹시 나 잡아줄 사람(try/catch) 있어?"** 라고 물어봅니다:

```typescript
function a() {
  b();  // c에서 에러남 → b한테 물어봄 → b도 catch 없음 → a한테 물어봄
}

function b() {
  c();  // c에서 에러남 → b한테 물어봄 "catch 있어?" → 없음
}

function c() {
  throw new Error('터졌다!');  // 💥 에러 발생!
}

a();
```

```
💥 c()에서 에러 발생!

  ┌─────┐
  │ c 💥│  "나 에러났어! 누가 catch 해줘!"
  │  b  │  "나한테 try/catch 없는데... 위로 넘긴다"
  │  a  │  "나한테도 try/catch 없는데... 위로 넘긴다"
  └─────┘
     ↓
  콜스택 맨 바닥까지 왔는데 아무도 안 잡아줌
     ↓
  ┌──────────────────┐
  │  window.onerror  │  ← JS 엔진의 최후의 보루
  └──────────────────┘
     ↓
  ┌──────────────────┐
  │  Sentry SDK      │  ← 여기에 리스너를 걸어둔 것
  └──────────────────┘
```

만약 `b()`에 try/catch가 있었다면:

```typescript
function b() {
  try {
    c();  // c에서 에러남
  } catch (error) {
    // 🎉 여기서 잡힘! 더 이상 위로 안 올라감
    console.log('내가 잡았다:', error.message);
  }
}
```

```
💥 c()에서 에러 발생!

  ┌─────┐
  │ c 💥│  "나 에러났어!"
  │ b 🎉│  "내 try/catch가 잡았다! 여기서 처리 끝."
  │  a  │  (에러가 여기까지 안 옴. 모르고 지나감)
  └─────┘
  
  → window.onerror까지 안 감
  → Sentry SDK도 모름 (handled:true로 직접 보내지 않는 한)
```

**그래서 "콜스택을 타고 올라간다" = 에러가 접시를 하나씩 내리면서 "누가 잡아줄 사람 있어?"를 묻는 과정입니다.**

---

## 3. 비동기에서 콜스택이 왜 비어있고, 왜 window.onerror로 못 가는지

이게 가장 어려운 부분인데, 아주 천천히 갑니다.

### 핵심 개념: JS는 한 번에 하나만 한다

```
JS 엔진 = 요리사 1명인 식당

주문 1: 파스타 만들기 → 만든다 → 완료
주문 2: 스테이크 만들기 → 만든다 → 완료
주문 3: 샐러드 만들기 → 만든다 → 완료

한 번에 하나씩만 요리함!
```

### 근데 fetch()는 시간이 오래 걸림

```
fetch('서버에서 상품 가져와')
→ 서버 응답 올 때까지 1~3초 걸림
→ 이 동안 요리사가 멍때리면 안 됨!
```

그래서 JS는 이렇게 합니다:

```
요리사: "fetch 주문 들어왔다. 서버한테 전화 걸어놓고,
         응답 오면 그때 다시 불러줘.
         나는 다음 주문 먼저 할게."
```

### 실제로 어떻게 되는지 타임라인

```typescript
function loadProducts() {         // ← 1번
  fetch('/api/products')          // ← 2번: 서버에 요청 보내고, "나중에" 처리
    .then(res => res.json())      // ← 이건 "나중에" 실행될 코드
    .then(data => {
      throw new Error('터짐!');   // ← 이것도 "나중에" 실행될 코드
    });
  
  console.log('다음 일 한다');     // ← 3번: fetch 안 기다리고 바로 실행
}

loadProducts();                    // ← 0번
console.log('끝');                 // ← 4번
```

시간 순서:

```
⏰ 시각 0ms: loadProducts() 호출
   콜스택: [ loadProducts ]

⏰ 시각 1ms: fetch() 호출 → 서버에 요청 보냄 → "나중에 알려줘" 등록
   콜스택: [ loadProducts ]

⏰ 시각 2ms: console.log('다음 일 한다') 실행
   콜스택: [ loadProducts ]

⏰ 시각 3ms: loadProducts() 끝
   콜스택: [ ] ← 🔥 비어있음!

⏰ 시각 4ms: console.log('끝') 실행
   콜스택: [ ] ← 🔥 비어있음!

   ... 시간이 흐름 ...

⏰ 시각 500ms: 서버 응답 도착! "아까 등록한 .then() 실행해야지"
   콜스택: [ ] ← 🔥 여전히 비어있음! loadProducts는 이미 끝남!
   
   JS 엔진이 .then() 콜백을 콜스택에 올림:
   콜스택: [ then 콜백 ]
   
   throw new Error('터짐!') 발생! 💥
```

### 왜 `window.onerror`로 못 가는가

**동기 에러일 때:**

```
  ┌─────────────┐
  │ c() 💥      │  에러!
  │ b()         │  catch 없음 → 위로
  │ a()         │  catch 없음 → 위로
  │ (전역 코드)  │  catch 없음 → 위로
  └─────────────┘
         ↓
  window.onerror  ← 콜스택 바닥의 연장선에 있음
```

에러가 접시를 하나씩 내리면서 올라갈 수 있는 이유: **접시들이 쌓여있으니까!**

**비동기 에러일 때:**

```
  .then() 콜백이 실행될 때의 콜스택:
  
  ┌─────────────┐
  │ then 콜백 💥│  에러!
  │             │  ← 그 아래에 아무것도 없음!!!
  └─────────────┘
  
  loadProducts()? → 이미 500ms 전에 끝남. 접시 치워짐.
  a(), b(), c()? → 그런 거 없음. 콜스택에 없음.
```

**올라갈 접시가 없습니다!** `.then()` 콜백은 혼자서 새로운 접시 위에서 시작했기 때문에, 에러가 올라갈 곳이 `window.onerror`까지의 경로가 없는 것입니다.

```
동기: 에러 → c → b → a → 전역 → window.onerror  ✅ 길이 있음

비동기: 에러 → then콜백 → ???  ❌ 길이 끊김!
```

### 그래서 `window.onunhandledrejection`이 태어남

JS를 만든 사람들도 이 문제를 알았습니다:

> "Promise 안에서 에러가 나면 window.onerror로 못 가네... Promise 전용 안전망을 하나 더 만들자!"

그게 `window.onunhandledrejection`입니다.

```
┌─────────────────────────────────────────────────────┐
│                     JS 엔진                          │
│                                                      │
│  동기 에러 안전망:  window.onerror                    │
│    → 콜스택을 타고 올라온 에러를 잡음                  │
│                                                      │
│  비동기(Promise) 에러 안전망: window.onunhandledrejection │
│    → 콜스택 경로가 없는 Promise 에러를 잡음            │
│                                                      │
│  Sentry SDK는 둘 다 리스너를 걸어둠:                   │
│    window.addEventListener('error', ...)             │
│    window.addEventListener('unhandledrejection', ...)│
│                                                      │
└─────────────────────────────────────────────────────┘
```

### 전체 그림

```
        에러 발생!
        ┌────┐
        │ 💥 │
        └──┬─┘
           │
     ┌─────┴──────┐
     │             │
  동기 에러     Promise 안 에러
     │             │
  콜스택을      콜스택이
  타고 올라감   비어있음
     │             │
  누가 catch?   누가 .catch()?
     │             │
  ┌──┴──┐      ┌──┴──┐
  │     │      │     │
 있음  없음   있음  없음
  │     │      │     │
 끝!    │     끝!    │
        ↓            ↓
  window.onerror    window.onunhandledrejection
        │            │
        └─────┬──────┘
              │
         Sentry SDK
         "내가 둘 다 주울게"
              │
         Sentry 서버로 전송
```

그래서 Sentry 대시보드에서:

- `error.mechanism:onerror` = 동기 에러가 안 잡힌 것
- `error.mechanism:onunhandledrejection` = Promise 에러가 안 잡힌 것

이 두 가지 경로로 잡힌 에러가 모두 `error.handled:false`가 됩니다.